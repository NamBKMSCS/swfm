# Force pnpm to use a flat node_modules structure
# This is necessary for Docker multi-stage builds
node-linker=hoisted

# Auto-install peers
auto-install-peers=true

# Strict peer dependencies
strict-peer-dependencies=false
